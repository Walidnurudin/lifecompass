{
  "name": "lifecompass-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.5.2",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "postinstall": "if [ -z \"$VERCEL\" ]; then cd backend && go mod download; fi",
    "dev": "turbo dev",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
